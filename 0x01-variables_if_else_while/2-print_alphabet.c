#include <stdio.h>

/**
 * main - A program that prints the alphabet in lowercase
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char alp[26] "rswxyz";
	int i;

	for (i = 0; i < 2 ; i++)
	{
	putchar(alp[i]);
	}
	putchar('\n');
	return (0);
}
